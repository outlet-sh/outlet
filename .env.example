# ===========================================
# Outlet.sh Configuration
# ===========================================
# Copy this file to .env and fill in your values
# Admin account is created via web UI at /setup on first launch

# ---------------------------------------------
# Required Settings
# ---------------------------------------------

# App Configuration
APP_BASE_URL=http://localhost:8888      # Base URL for the application
APP_DOMAIN=localhost                     # Domain name (used for cookies, CORS)
PRODUCTION_MODE=                         # Set to "true" for HTTPS/Let's Encrypt
PLATFORM_NAME=Outlet                     # Your platform name (shown in UI)

# Database (SQLite with WAL mode)
DATABASE_PATH=./data/outlet.db          # Path to SQLite database file

# JWT Authentication
# IMPORTANT: Generate secure random strings for production!
# Generate with: openssl rand -base64 32
JWT_SECRET=your-access-token-secret-min-32-chars
JWT_REFRESH_SECRET=your-refresh-token-secret-min-32-chars

# Encryption Key for storing sensitive data (SMTP passwords, API keys)
# MUST be 32 bytes as 64 hex characters
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# WebSocket URL (for real-time features)
WEBSOCKET_URL=ws://localhost:8888

# ---------------------------------------------
# Optional Features
# ---------------------------------------------

# Google OAuth & Calendar Integration
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
GOOGLE_REDIRECT_URL=http://localhost:8888/api/oauth/google/callback

# Stripe Payments - enables subscription billing
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_BASE_URL=http://localhost:8888
STRIPE_SUCCESS_URL=http://localhost:8888/thank-you
STRIPE_CANCEL_URL=http://localhost:8888

# Zoom Integration - enables meeting scheduling
ZOOM_API_KEY=
ZOOM_API_SECRET=
ZOOM_ACCOUNT_ID=

# Sales Agent Email - receives sales notifications
SALES_AGENT_EMAIL=

# ---------------------------------------------
# Production Settings
# ---------------------------------------------
# When PRODUCTION_MODE=true:
# - HTTPS with Let's Encrypt auto-certificates
# - APP_DOMAIN must be a real domain
# - APP_BASE_URL should be https://yourdomain.com
# - Generate strong JWT secrets
# - Configure proper SMTP settings in the admin UI

# ---------------------------------------------
# SMTP Configuration
# ---------------------------------------------
# SMTP settings are configured via the web UI at:
# /settings/email (platform-level)
# Each organization can also have its own email settings
